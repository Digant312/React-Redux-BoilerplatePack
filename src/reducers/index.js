import { combineReducers } from 'redux';

// Import all reducers here and combine them in rootReducer

const rootReducer = combineReducers({
    // weather: WeatherReducer
});
  
export default rootReducer;